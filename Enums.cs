using System;
using System.Collections.Generic;
using System.Text;

namespace Credit
{
    class Enums
    {
        public enum RiskType { LowRisk, MediumRisk, HighRisk }

        public enum RiskSector { Private, Public }
    }
}
